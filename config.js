// database config
const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'bee-unlimited',
    },
    listPerPage: 10,
    connectionLimit: 10
}

module.exports = config