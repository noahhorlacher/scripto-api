// database config
const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'scripto',
    },
    listPerPage: 10,
    connectionLimit: 10
}

module.exports = config